docs/agent/ORCHESTRATOR.md の手順に従い、docs/agent/TASKS.md のタスクを実装する。
完了条件: (1) この実行で新たに {{MAX_TASKS}} 件のタスクを [x] にした、または着手可能なタスク([ ] かつ依存がすべて [x])が残っていない。
(2) 最終確認の手順を実行し、直近の npm run verify の成功が会話に出力されている。
(3) git status --porcelain の出力が空であることが会話に示されている。
(4) main ブランチへのコミットや push を行っていない。
{{MAX_TURNS}} ターンに達した場合は、その時点で最終確認を行って終了する。
